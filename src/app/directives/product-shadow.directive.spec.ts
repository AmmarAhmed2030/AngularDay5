import { ProductShadowDirective } from './product-shadow.directive';

describe('ProductShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductShadowDirective();
    expect(directive).toBeTruthy();
  });
});
