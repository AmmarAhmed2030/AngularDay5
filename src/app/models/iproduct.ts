export interface IProduct {
  id: string;
  name: string;
  title: string;
  quantity: number;
  description: string;
  price: number;
  image: string;
  category: string;
  purchaseDate: any;
  isPurchased: boolean;
}
